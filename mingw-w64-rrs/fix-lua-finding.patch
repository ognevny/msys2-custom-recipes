--- a/simulator/scenario-manager/CMakeLists.txt
+++ b/simulator/scenario-manager/CMakeLists.txt
@@ -12,7 +12,7 @@
 
 find_package(Qt6 COMPONENTS Core REQUIRED)
 
-if(WIN32 OR APPLE)
+if(FALSE)
     find_package(lua CONFIG REQUIRED)
 else()
     find_package(Lua 5.4 REQUIRED)
@@ -31,7 +31,7 @@
 
 target_link_libraries(${TARGET} Qt6::Core)
 
-if(WIN32 OR APPLE)
+if(FALSE)
     # On macOS: ensure Lua headers from lua-cmake are found before system Lua 5.5
     if(APPLE)
         get_target_property(_LUA_INC lua::lua INTERFACE_INCLUDE_DIRECTORIES)
--- a/simulator/simulator/CMakeLists.txt
+++ b/simulator/simulator/CMakeLists.txt
@@ -22,7 +22,7 @@
 # Пути к заголовкам и сорцам
 file (GLOB INCLUDES "./include/*.h")
 file (GLOB SOURCES "./src/*.cpp")
-if(WIN32 OR APPLE)
+if(FALSE)
     find_package(lua CONFIG REQUIRED)
 else()
     find_package(Lua 5.4 REQUIRED)
@@ -30,7 +30,7 @@
 
 # Компиляция приложения
 add_executable (${TARGET} ${SOURCES} ${INCLUDES})
-if(WIN32 OR APPLE)
+if(FALSE)
     target_link_libraries(${TARGET} lua::lua)
 else()
     target_link_libraries(${TARGET} ${LUA_LIBRARIES})
--- a/simulator/model/CMakeLists.txt
+++ b/simulator/model/CMakeLists.txt
@@ -28,7 +28,7 @@
 file (GLOB INCLUDES "./include/*.h")
 file (GLOB SOURCES "./src/*.cpp")
 
-if(WIN32 OR APPLE)
+if(FALSE)
     find_package(lua CONFIG REQUIRED)
 else()
     find_package(Lua 5.4 REQUIRED)
@@ -36,7 +36,7 @@
 
 # Компиляция библиотеки
 add_library (${TARGET} SHARED ${SOURCES} ${INCLUDES})
-if(WIN32 OR APPLE)
+if(FALSE)
     target_link_libraries(${TARGET} lua::lua)
 else()
     target_link_libraries(${TARGET} ${LUA_LIBRARIES})
