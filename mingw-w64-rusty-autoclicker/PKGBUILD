# Maintainer: Maksim Bondarenkov <maksapple2306@gmail.com>

_realname=rusty-autoclicker-git
pkgbase=mingw-w64-${_realname}
pkgname=${MINGW_PACKAGE_PREFIX}-${_realname}
pkgver=v2.4.0.r9.g806ca4e
pkgrel=1
pkgdesc="A pure Rust portable auto clicker built for Linux, macOS & Windows (mingw-w64)"
arch=('any')
mingw_arch=('ucrt64' 'clang64' 'clangarm64')
url="https://github.com/MrTanoshii/rusty-autoclicker"
license=('spdx:CC0')
makedepends=("${MINGW_PACKAGE_PREFIX}-rust" 'git')
provides=("${pkgname%-git}")
conflicts=("${pkgname%-git}")
options=('!strip')
source=('git+https://github.com/MrTanoshii/rusty-autoclicker.git#commit=806ca4e9e8faf029bb5f5e4afa1dddc686d8b52d')
sha256sums=('688f8fb06dde1efdc3c39df631eca9e25d387824b48bca5aa4b7e69fe646f80f')

pkgver() {
	cd "${_realname%-git}"
  git describe --long --tags --abbrev=7 | sed 's/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd "${_realname%-git}"

  cargo fetch --locked --target "${RUST_CHOST}"
}

build() {
	cd "${_realname%-git}"

  cargo build --frozen --release
}

package() {
	cd "${_realname%-git}"

  install -Dm755 target/release/${_realname%-git} "${pkgdir}${MINGW_PREFIX}/bin/${_realname%-git}"
}
